<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="Theme.NovaSec" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/nova_background</item>
        <item name="android:navigationBarColor">@color/nova_background</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Splash Screen Theme -->
    <style name="Theme.NovaSec.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/nova_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>
        <item name="postSplashScreenTheme">@style/Theme.NovaSec</item>
        <item name="android:statusBarColor">@color/nova_background</item>
        <item name="android:navigationBarColor">@color/nova_background</item>
    </style>
</resources>
